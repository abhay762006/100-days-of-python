# 🧮 Python Calculator

A simple command-line calculator built using Python. This project allows users to perform basic arithmetic operations through an interactive terminal interface.

This project was created as part of my **100 Days of Python** learning journey to strengthen my programming fundamentals and problem-solving skills.

---

## 📌 About The Project

The calculator takes two numbers from the user and allows them to choose an arithmetic operation.

The available operations are:

- Addition `+`
- Subtraction `-`
- Multiplication `*`
- Division `/`

After performing a calculation, the user can choose whether they want to perform another calculation or exit the program.

---

## ✨ Features

- Perform basic arithmetic calculations
- Supports decimal numbers
- Interactive command-line interface
- Performs multiple calculations in one execution
- Uses separate functions for each mathematical operation
- Handles invalid operation choices

---

## 🛠️ Technologies Used

- **Python 3**
- Command Line / Terminal

---

## 🧠 Concepts Practiced

This project helped me practice and understand:

- Functions
- Function parameters
- `return` statements
- User input with `input()`
- Type conversion using `float()`
- `if`, `elif`, and `else`
- `while` loops
- Arithmetic operators
- Basic program flow and logic

---

## ⚙️ How It Works

The program follows this basic flow:

```text
Start
  ↓
Ask user if they want to calculate
  ↓
Enter first number
  ↓
Enter second number
  ↓
Select an operation
  ↓
Perform calculation
  ↓
Display result
  ↓
Ask whether to continue
  ↓
Exit or calculate again